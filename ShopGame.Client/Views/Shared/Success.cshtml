@model string;
@inject IViewLocalizer Localizer
<div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <div class="card border-success">
            <div class="card-body text-center">
                <h1 class="text-success mb-4">@Localizer["Suc"]</h1>
                <p class="lead mb-4">@Localizer["SName"]</p>
                <a class="btn btn-success" href="@Url.Action("Index", @Model)">@Localizer["HomeP"]</a>
            </div>
        </div>
    </div>
</div>