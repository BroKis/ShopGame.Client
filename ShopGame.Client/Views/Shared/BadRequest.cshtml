@model string;
@inject IViewLocalizer Localizer
<div class="row">
    <div class="col-md-6 offset-3">
        <h1 class="text-info">@Localizer["ReqName"]:(</h1>
        <hr />
        <a class="btn btn-outline-success" asp-controller="@Model" asp-action="Index">@Localizer["HName"]</a>
    </div>
</div>