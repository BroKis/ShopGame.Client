@inject IViewLocalizer Localizer
@model string;
<div class="row">
    <div class="col-md-6 offset-3">
        <h1 class="text-info">@Localizer["Header"]</h1>
        <hr />
        <a class="btn btn-outline-success" asp-controller="@Model" asp-action="Index">@Localizer["HomePage"]</a>
    </div>
</div>